; Comments — themed grey
(comment) @comment

; Dividers (--- Setup ---, --- Map ---) — themed as constants
(divider) @constant

; Header directive keywords (Start, Scope, Exclude, etc.)
(directive_keyword) @function

; Step keywords (Visit, Goto, Click, etc.)
(step_keyword) @keyword

; Quoted strings
(quoted_string) @string
